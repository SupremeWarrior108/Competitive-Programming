#include <bits/stdc++.h>
using namespace std;

void solve() {
    int t;  
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        cout << 1;
        for (int i = 1; i < n; i++) {
            cout << 0; 
        }
        cout << "\n";
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}
